patchright>=1.50.0
curl_cffi>=0.5.0
geopy>=2.4.0
pandas>=2.0.0
numpy>=1.24.0
requests>=2.28.0
pytest>=7.0.0
sgprop @ git+https://github.com/ong6/sgprop@dec6e1a  # URA API client (ura_api.py); needs URA_ACCESS_KEY. Pinned: bump deliberately
